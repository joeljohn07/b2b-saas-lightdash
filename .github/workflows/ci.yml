# ============================================================
# AUTOMATION: CI
# WHAT IT DOES: Runs tests on push and PR to main
# WHAT IT MODIFIES: Nothing — read-only check
# AUTO-COMMITS: No
# HOW TO DISABLE: Delete this file
# ============================================================
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Run tests
        run: echo 'No test runner configured — add your test command here'
